# Text ('+' : current line)
fg:#D9D9D9
fg+:#D9D9D9

# Background ('+' : current line)
bg:#000000
bg+:#3F3F3F

# Preview window text & background
preview-fg:#D9D9D9
preview-bg:#000000

# Highlighted substrings ('+' : current line)
hl:#719872
hl+:#98BC99

# Gutter on the left (defaults to bg) 
gutter:#000000

# Info line (match counters)
info:#E12672

# Header
header:#eeee55

# Border around the window (--border and --preview)
border:#6B6B6B

# Prompt
prompt:#98BEDE

# Pointer to the current line
pointer:#E12672

# Multi-select marker
marker:#E17899

# Streaming input indicator
spinner:#98BC99


# ---------------- Document ----------------#
# Colors:
#   -1 :  Default terminal foreground/background color
#   #rrggbb    24-bit colors

# Decoration (Only foreground colors; Depends on Terminal)
#   bold
#   underline
#   reverse
#   dim
#   italic
# Example:
#   fg:underline

# BASE SCHEME  (default: dark on 256-color terminal, otherwise 16):
#   dark    Color scheme for dark 256-color terminal
#   light   Color scheme for light 256-color terminal
#   16      Color scheme for 16-color terminal
#   bw      No colors (equivalent to --no-color)
# Example:
#   dark
# ------------------------------------------#
